import { StyleSheet } from 'react-native'
import { Helpers, Metrics, Fonts, Colors } from '../../Theme'

export default StyleSheet.create({
    imageContainer: { width: "100%", height: "90%", resizeMode: "contain" }
})
